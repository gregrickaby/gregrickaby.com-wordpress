export interface Slide {
  $thumb: {
    origTarget: {
      alt: string
      title: string
    }
  }
  $trigger: {
    origTarget: {
      alt: string
      title: string
    }
  }
  caption: string
  src: string
  thumb: string
  type: string
}
